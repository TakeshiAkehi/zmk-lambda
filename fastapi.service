[Unit]
Description=FastAPI App
After=network.target

[Service]
ExecStart=/home/ec2-user/zmk-lambda/.env/bin/uvicorn main:app --host 127.0.0.1 --port 8000
WorkingDirectory=/home/ec2-user/zmk-lambda
Restart=always

[Install]
WantedBy=multi-user.target